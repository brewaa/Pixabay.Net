namespace Pixabay.Net
{
    public enum CategoryEnum { fashion, nature, backgrounds, science, education, people, feelings, religion, health, places, animals, industry, food, computer, sports, transportation, travel, buildings, business, music }

    public enum ImageTypeEnum { all, illustration, photo, vector }

    public enum LanguageCodeEnum { en, cs, da, de, es, fr, id, it, hu, nl, no, pl, pt, ro, sk, fi, sv, tr, vi, th, bg, ru, el, ja, ko, zh }

    public enum OrderEnum { popular, latest }

    public enum OrientationEnum { all, horizontal, vertical }

    public enum QueryTypeEnum { image, video }

    public enum ResponseGroupEnum { image_details, high_resolution }

    public enum VideoTypeEnum { all, film, animation }
}
